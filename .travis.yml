language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gakalmar/basic-react-app-practice -f Dockerfile.dev .

script:
  - docker run -e CI=true gakalmar/basic-react-app-practice npm run test